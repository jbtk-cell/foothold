---
title: A Real Project
summary: Build one program across four lessons, the way software actually gets written.
---

The last four lessons build a single thing: a task tracker with a data model,
operations, storage and a command interface.

Each lesson takes the previous one further, so what you finish with is around
a hundred lines that hold together. That is the difference between knowing the
syntax and being able to build something.
